{% extends 'base.html.twig' %}

{% block title %}Hello ManifController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! </h1>
    {# templates/article/recent_list.html.twig #}
{# the contents of this variable are sent to the Web Debug Toolbar
   instead of dumping them inside the page contents #}
{% dump manif %}

{% for manif in manif %}
    {# the contents of this variable are dumped inside the page contents
       and they are visible on the web page #}
    {{ dump(manif) }}

    <a href="/manif/{{ manif.slug }}">
        {{ manif.title }}
    </a>
{% endfor %}
            {% for manif in manif %}
            <tr>
                <td>{{ manif.genre }}</td>
                <td>{{ manif.titre }}</td>
                <td>{{ manif.descript }}</td>
                <td>{{ manif.casting }}</td>
                <td>{{ manif.prix }}</td>
                <td>{{ manif.date }}</td>
                <td>{{ manif.horaire }}</td>
                <td><a href="../manifestation" id="reserver">Réserver</a></td>
{% endfor %}



    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ '/var/www/test/src/Controller/ManifController.php'|file_link(0) }}">src/Controller/ManifController.php</a></code></li>
        <li>Your template at <code><a href="{{ '/var/www/test/templates/manif/index.html.twig'|file_link(0) }}">templates/manif/index.html.twig</a></code></li>
    </ul>
</div>
{% endblock %}
